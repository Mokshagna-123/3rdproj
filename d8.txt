to delete an node at any position in a double linked list
//
struct node*curr=head;
if(curr==NULL)
 return head;
 for(i=0;curr && i<pos;i++)
 {curr=curr->next;}
 if(curr->prev)
    {curr->prev->next=curr->next;}
    if(curr->next)
    {curr->next->prev=curr->prev;}
    if(curr==head)
    {curr->next=head;}
    return head;
//
    