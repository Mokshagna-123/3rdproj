inserting a node in an empty circular linked list
//
struct node*createnode(int data)
{
   struct node*newnode=(struct node*)malloc(sizeof(struct node));
   newnode->data=data;
   newnode->next=newnode;
   return newnode;
}
struct node*insertinemptylist(struct node*last)
{   while(last!=NULL)
     {return last;}
    struct node*temp=createnode(2);
       last=temp;
       return last;
}
void printlist(struct node*last)
{
    struct node*head=last->next;
    while(1)
    { printf("%d",head->data);
      head =head->next;
        if(last->next==head)
        break;
    }
}
//
insert a node at beggining of a circular linked list
//
struct node*insert at the beggining(struct node*last)
{   
   struct node*temp=creatnode(value);
    if(last==NULL)
     {temp->next=temp;
         return temp;
     }
     temp->next=last->next;
     last->next=temp;
     return last;
}
void printlist(struct node*last)
{if (last==NULL)
    return;
    struct node*head=last->next;
    while(1)
    {printf("%d",head->data);
        head=head->next;
        if(head==last->next)
        break;
    }
}
//