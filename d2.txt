

code to create a node in single linked list;
felt amazing!!


//defination of a node//
  struct node{
      int data;
      struct node *next;
  }
  //creating a new node//
  struct node *newnode(int data)
   {struct node *temp=malloc(sizeof(struct node));
   
    if(temp==NULL)
     {printf("memory allocation is failed");
     return NULL;
        
     }
       temp->data=required date to allocate in this node;
       temp->next=NULL;
       
       return temp;
   }