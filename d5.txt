delete a node at a particular position
//
main code
struct node*deleteposition(struct node*head,int pos)
{struct node*curr=head;
    for(i=0;i<curr-1;i++)
    {curr=curr->next;}
    
    struct node*temp=curr->next;
    curr->next=curr->next->next;
    free(temp);
    return head;
}
//
create a new node in double linked list
 //
 struct node*create node(int data)
 {struct node*new_node=(struct node*)malloc(sizeof(struct node));
    new_node->data=data;
    new_node->prev=NULL;
    new_node->next=NULL;
    return new_node;
 }
 //