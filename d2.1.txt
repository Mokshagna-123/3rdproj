*stack*
to create a stack
//
  struct Stack(int cap)
  {
      int cap,top;
      int*a;
  }
 struct Stack*create stack(int cap)
 {
     struct Stack*stack=(struct Stack*)malloc(sizeof(struct Stack));
     struct Stack->cap=cap;
     struct Stack->top=-1;
     struct Stack->a=(int*)malloc(cap * sizeof(int));
 }
 //
