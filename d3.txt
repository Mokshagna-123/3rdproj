transverse along the single linked list
//
void tranverselinkedlist(struct node*head)
{struct node*current=head;
    while(current!=NULL)
    {printf("%d",current->data);
        current=current->next;
    }
}
//

searching the required element in the linked list

//
bool searchlinkedlist(struct node*head,int target)
  {
      while(head!=NULL)
      {
          if(head->data==target)
          {return true;}
          
          head=head->next;
      }
      
      return false;
  }
//

inserting a node at beginning

//
struct node*insertatbeginning(struct node*head)
 {
     struct node*newhead=newnode(data);
     newnode->next=head;
     head=newhead;
  return head;
 }
 //